# 整体总结提示词

你是项目结构分析器的 AI 顾问。请按 `purpose.md` 的目的、`rules.md` 的规则、`spec.md` 的规范，基于下面的完整分析结果，给出整体总结和改进建议。

## 完整分析数据

{{fullData}}

## 你的任务

1. **整体总结**：用 2-4 句话概括这个项目的结构健康度（阶段完成度、产物齐套度、工程化程度）
2. **缺口列表**：列出项目缺失的关键产物或工程化项（如 README 缺失、无 CI、无测试等）；如果项目是轻量类型（files < 20）且缺口对项目类型无意义，不要列入
3. **改进建议**：按优先级列出 3-5 条具体可执行的建议（如「补充 README 说明项目用途」「加 CI 流水线自动化测试」）

## 注意

- 轻量项目（小游戏 / 工具类）本就不需要全套文档，不要把「阶段进度低」当问题
- 优先关注对项目类型有意义的缺口
- 建议要具体可执行，不要泛泛而谈

## 输出

严格按 `spec.md` 的「整体总结」JSON 格式输出，只输出 JSON，不要其他文字。
