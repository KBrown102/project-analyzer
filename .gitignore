# 依赖与打包产物
node_modules/
build-electron/node_modules/
build-electron/dist-out/
# 本地打包时才生成；CI 不装这边依赖，入库只会白占 189KB
build-electron/package-lock.json
# 开发态 userData 隔离目录（main.js app.setPath 重定向到此，避免污染 %APPDATA%）
build-electron/.data/

# 杂项
*.log
.DS_Store
Thumbs.db

# 本地临时文件（曾经误入库过一次，2026-09-18 清理，别再放进来）
commit-msg.txt
